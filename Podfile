# Uncomment the next line to define a global platform for your project
# platform :ios, '9.0'

target 'bitnance assignment' do
  # Comment the next line if you don't want to use dynamic frameworks
  use_frameworks!
  # Pods for bitnance assignment
  pod 'CocoaDebug', '1.4.8'#, :configurations => ['Debug']
  pod 'Alamofire', '~> 5.2'
  pod 'Kingfisher', '~> 5.0'
  pod 'Moya-ObjectMapper/RxSwift'
  pod "RSLoadingView"

  target 'bitnance assignmentTests' do
    inherit! :search_paths
    # Pods for testing
  end

  target 'bitnance assignmentUITests' do
    # Pods for testing
  end

end
